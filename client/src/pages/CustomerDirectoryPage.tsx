import { trpc } from "@/lib/trpc";
import { ArrowUpRight, CalendarDays, Phone, Users } from "lucide-react";
import { useMemo, useState } from "react";
import { useLocation } from "wouter";
import { EmptyState, LoadingState, MetricCard, PageHeader, PrimaryButton, SearchField, StatusPill, Surface, TableFrame, TableHeader, TableRow } from "@/components/MarasiUI";

const money = (value: unknown) => `OMR ${Number(value || 0).toLocaleString("en-OM", { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
const fmt = (value: unknown) => value ? new Date(value as string).toLocaleDateString("en-GB", { day: "2-digit", month: "short", year: "numeric" }) : "—";

export default function CustomerDirectoryPage() {
  const [, setLocation] = useLocation(); const [query, setQuery] = useState(""); const [expandedId, setExpandedId] = useState<number | null>(null);
  const searchInput = { query: query.trim() || undefined };
  const { data: records = [], isLoading: customersLoading, isError: customersError } = trpc.platform.customers.search.useQuery(searchInput);
  const { data: purchaseRows = [], isLoading: purchasesLoading } = trpc.platform.tickets.purchaseList.useQuery({ query: query.trim() || undefined });
  const isLoading = customersLoading || purchasesLoading;
  const customers = useMemo(() => {
    const map = new Map<number, any>();
    (records as any[]).forEach((customer) => { if (customer.id) map.set(customer.id, { id: customer.id, name: customer.fullName || "Guest", phone: customer.phone || "—", email: customer.email || "", visits: [], total: 0 }); });
    (purchaseRows as any[]).forEach((row) => { const customer = row.customer || {}; if (!customer.id) return; const current = map.get(customer.id) || { id: customer.id, name: customer.fullName || "Guest", phone: customer.phone || "—", email: customer.email || "", visits: [], total: 0 }; let purchase = current.visits.find((entry: any) => entry.id === row.purchase.id); if (!purchase) { purchase = { id: row.purchase.id, visitDate: row.purchase.visitDate, totalAmount: row.purchase.totalAmount, ticketNumbers: [] }; current.visits.push(purchase); current.total += Number(row.purchase.totalAmount || 0); } if (row.line?.ticketNumber && !purchase.ticketNumbers.includes(row.line.ticketNumber)) purchase.ticketNumbers.push(row.line.ticketNumber); map.set(customer.id, current); });
    return Array.from(map.values()).map((customer) => ({ ...customer, visits: customer.visits.sort((a: any, b: any) => new Date(b.visitDate).getTime() - new Date(a.visitDate).getTime()) })).sort((a, b) => new Date(b.visits[0]?.visitDate || 0).getTime() - new Date(a.visits[0]?.visitDate || 0).getTime());
  }, [records, purchaseRows]);
  const totalVisits = customers.reduce((sum, customer) => sum + customer.visits.length, 0);
  return <><PageHeader eyebrow="Front office" title="Customer directory" description="Search customer profiles and every PRD ticket purchase by name, phone, or continuous ticket number." actions={<PrimaryButton onClick={() => setLocation("/tickets")}>New purchase <ArrowUpRight size={15} className="ml-2"/></PrimaryButton>}/><div className="grid gap-4 sm:grid-cols-3"><MetricCard icon={Users} label="Customer profiles" value={String(customers.length)} detail="Matching current search" tone="blue"/><MetricCard icon={CalendarDays} label="Purchase records" value={String(totalVisits)} detail="Grouped customer visits" tone="green"/><MetricCard icon={Phone} label="Search coverage" value="3 fields" detail="Name · phone · ticket" tone="amber"/></div><Surface className="mt-6"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><SearchField value={query} onChange={setQuery} placeholder="Search customer name, phone, or ticket number"/><div className="text-xs text-subtle">{isLoading ? "Searching…" : `${customers.length} profile${customers.length === 1 ? "" : "s"}`}</div></div></Surface><div className="mt-6">{customersError ? <Surface><EmptyState title="Could not load customers" description="Refresh the page or try again from the ticket desk." action={<PrimaryButton onClick={() => setLocation("/tickets")}>Open ticket desk</PrimaryButton>}/></Surface> : isLoading ? <LoadingState label="Searching customer history…"/> : customers.length ? <Surface className="p-0"><TableFrame className="border-0"><TableHeader><div className="grid grid-cols-[1.25fr_1fr_.65fr_auto] gap-3"><span>Customer</span><span>Last visit</span><span>Lifetime</span><span className="text-right">History</span></div></TableHeader>{customers.map((customer) => <div key={customer.id}><TableRow className="grid-cols-[1.25fr_1fr_.65fr_auto]"><div className="min-w-0"><div className="truncate font-medium text-ink">{customer.name}</div><div className="mt-1 truncate text-xs text-muted">{customer.phone}{customer.email ? ` · ${customer.email}` : ""}</div></div><div className="text-xs text-muted">{fmt(customer.visits[0]?.visitDate)}<div className="mt-1"><StatusPill tone="info">{customer.visits.length} {customer.visits.length === 1 ? "purchase" : "purchases"}</StatusPill></div></div><b className="text-sm font-medium">{money(customer.total)}</b><button onClick={() => setExpandedId(expandedId === customer.id ? null : customer.id)} className="justify-self-end rounded-full bg-fill px-3 py-2 text-[11px] font-semibold text-ink hover:bg-[#e8e8ed]">{expandedId === customer.id ? "Close" : "View"}</button></TableRow>{expandedId === customer.id && <div className="border-t border-divider bg-[#fbfbfd] px-4 py-4"><div className="mb-3 text-[10px] font-semibold uppercase tracking-[.14em] text-subtle">Purchase history</div><div className="grid gap-2">{customer.visits.slice(0, 8).map((visit: any) => <div key={visit.id} className="grid grid-cols-[1fr_.8fr_auto] items-center gap-3 rounded-xl bg-white px-3 py-2.5 text-xs"><div><b className="font-mono text-accent">{visit.ticketNumbers.join(" · ") || `Purchase #${visit.id}`}</b><div className="mt-1 text-muted">Waterpark purchase</div></div><span className="text-muted">{fmt(visit.visitDate)}</span><b>{money(visit.totalAmount)}</b></div>)}</div></div>}</div>)}</TableFrame></Surface> : <Surface><EmptyState title="No matching customers" description="Try a name, phone number, or ticket number. New profiles are created at the Ticket Desk." action={<PrimaryButton onClick={() => setLocation("/tickets")}>Open Ticket Desk</PrimaryButton>}/></Surface>}</div></>;
}
