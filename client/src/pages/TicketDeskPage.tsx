import { useMemo, useState } from "react";
import { Plus, Printer, Ticket, Trash2, UserRound } from "lucide-react";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";
import { EmptyState, Field, PageHeader, PrimaryButton, SearchField, SecondaryButton, SelectField, StatusPill, Surface, TableFrame, TableHeader, TableRow, TextField, cx } from "@/components/MarasiUI";
import { TicketReceipt, type TicketReceiptData } from "@/components/TicketReceipt";
import { Textarea } from "@/components/ui/textarea";

const today = new Date().toISOString().slice(0, 10);
const money = (value: unknown) => `OMR ${Number(value || 0).toLocaleString("en-OM", { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
const dateLabel = (value: unknown) => value ? new Date(value as string).toLocaleDateString("en-GB", { day: "2-digit", month: "short", year: "numeric" }) : "—";
const freeLabels: Record<string, string> = { under_two: "Under 2 years", person_of_determination: "Person of determination", senior: "Retiree / senior" };
type TicketLine = { id: number; rateId: string; ticketType: "waterpark" | "companion"; freeEntryCategory: "" | "under_two" | "person_of_determination" | "senior" };
type FormState = { customerId: string; customerName: string; customerPhone: string; visitDate: string; paymentMethod: "cash" | "card" | "bank" | "mixed"; notes: string };
const blankForm: FormState = { customerId: "", customerName: "", customerPhone: "", visitDate: today, paymentMethod: "cash", notes: "" };
const blankLine = (id: number): TicketLine => ({ id, rateId: "", ticketType: "waterpark", freeEntryCategory: "" });

function Step({ number, title, detail, active }: { number: string; title: string; detail: string; active?: boolean }) {
  return <div className={cx("flex items-start gap-3 rounded-2xl border p-4", active ? "border-[#cce5ff] bg-[#f0f7ff]" : "border-divider bg-white")}><span className={cx("grid h-7 w-7 shrink-0 place-items-center rounded-full text-xs font-semibold", active ? "bg-accent text-white" : "bg-fill text-body")}>{number}</span><div><b className="block text-xs text-ink">{title}</b><span className="mt-1 block text-[11px] leading-4 text-muted">{detail}</span></div></div>;
}

function toReceiptData(created: any): TicketReceiptData {
  return {
    customerName: created.customer.fullName,
    customerPhone: created.customer.phone || "",
    visitDate: created.purchase.visitDate,
    baseSubtotal: created.purchase.baseSubtotal,
    discountAmount: created.purchase.discountAmount,
    vatAmount: created.purchase.vatAmount,
    totalAmount: created.purchase.totalAmount,
    lines: created.lines.map((line: any) => ({
      ticketNumber: line.ticketNumber, ticketType: line.ticketType, freeEntryCategory: line.freeEntryCategory,
      basePrice: line.basePrice, discountAmount: line.discountAmount, vatAmount: line.vatAmount, totalAmount: line.totalAmount,
    })),
  };
}

export default function TicketDeskPage() {
  const utils = trpc.useUtils();
  const [customerQuery, setCustomerQuery] = useState("");
  const [ticketQuery, setTicketQuery] = useState("");
  const [form, setForm] = useState<FormState>(blankForm);
  const [lines, setLines] = useState<TicketLine[]>([blankLine(1)]);
  const [created, setCreated] = useState<any>(null);
  const [receiptWidth, setReceiptWidth] = useState<"80" | "58">("80");
  const { data: catalog } = trpc.platform.tickets.prdCatalog.useQuery();
  const rates = (catalog?.rates || []) as any[];
  const { data: customers = [], isLoading: customersLoading } = trpc.platform.customers.search.useQuery({ query: customerQuery.trim() || undefined });
  const { data: purchaseRows = [], isLoading: purchasesLoading } = trpc.platform.tickets.purchaseList.useQuery({ query: ticketQuery.trim() || undefined });
  const validLines = lines.filter((line) => rates.some((rate) => String(rate.id) === line.rateId));
  const previewLines = lines.map((line) => ({ rateId: Number(line.rateId || 0), ticketType: line.ticketType, freeEntryCategory: line.freeEntryCategory || null })).filter((line) => line.rateId > 0);
  const { data: pricing } = trpc.platform.tickets.purchasePreview.useQuery({ lines: previewLines }, { enabled: previewLines.length === lines.length && lines.length > 0 });
  const groupedPurchases = useMemo(() => {
    const map = new Map<number, any>();
    (purchaseRows as any[]).forEach((row) => { const id = row.purchase.id; const current = map.get(id) || { ...row, lines: [] }; if (row.line) current.lines.push(row.line); map.set(id, current); });
    return Array.from(map.values());
  }, [purchaseRows]);
  const issue = trpc.platform.tickets.purchaseCreate.useMutation({
    onSuccess: (result: any) => { setCreated(result); setForm((current) => ({ ...blankForm, visitDate: current.visitDate })); setLines([blankLine(1)]); setCustomerQuery(""); utils.platform.tickets.purchaseList.invalidate(); utils.platform.customers.search.invalidate(); utils.platform.finance.invalidate(); toast.success(`${result.lines.length} ticket${result.lines.length === 1 ? "" : "s"} issued`); },
    onError: (error) => toast.error(error.message),
  });
  const selectCustomer = (customer: any) => { setForm((current) => ({ ...current, customerId: String(customer.id), customerName: "", customerPhone: "" })); setCustomerQuery(""); };
  const updateLine = (id: number, patch: Partial<TicketLine>) => setLines((current) => current.map((line) => line.id === id ? { ...line, ...patch } : line));
  const addLine = () => setLines((current) => [...current, blankLine(Math.max(...current.map((line) => line.id), 0) + 1)]);
  const removeLine = (id: number) => setLines((current) => current.length === 1 ? current : current.filter((line) => line.id !== id));
  const issuePurchase = () => { if (!form.customerId && (!form.customerName.trim() || !form.customerPhone.trim())) return toast.error("Select a customer or add a name and phone"); if (previewLines.length !== lines.length) return toast.error("Select a ticket type and approved price for every line"); issue.mutate({ customerId: form.customerId ? Number(form.customerId) : undefined, customerName: form.customerId ? undefined : form.customerName.trim(), customerPhone: form.customerId ? undefined : form.customerPhone.trim(), visitDate: form.visitDate, paymentMethod: form.paymentMethod, notes: form.notes.trim() || undefined, lines: previewLines }); };
  const printReceipt = (width: "80" | "58") => { setReceiptWidth(width); window.setTimeout(() => window.print(), 0); };

  return <>
    <PageHeader eyebrow="Front office · ticketing" title="Issue tickets" description="Build one purchase with Waterpark or Companion lines, apply the PRD rules, and print a normal receipt." actions={<StatusPill tone="info">5% VAT · server calculated</StatusPill>}/>
    <div className="mb-6 grid gap-3 md:grid-cols-3"><Step number="1" title="Customer" detail="Search a saved profile or add a walk-in." active={!form.customerId}/><Step number="2" title="Visitors" detail="Select ticket type and free-entry status per person." active={Boolean(form.customerId || form.customerName) && !created}/><Step number="3" title="Receipt" detail="Review discount, VAT, fees, and print." active={Boolean(created)}/></div>
    <div className="grid gap-6 xl:grid-cols-[.88fr_1.12fr]">
      <div className="space-y-6">
        <Surface><div className="mb-5 flex items-start justify-between gap-3"><div><h2 className="font-serif text-2xl tracking-[-.04em]">Customer</h2><p className="mt-1.5 text-xs leading-5 text-muted">One customer record anchors this purchase and its visit history.</p></div><UserRound size={19} className="text-accent"/></div><SearchField value={customerQuery} onChange={setCustomerQuery} placeholder="Search by name or phone"/><div className="mt-3 max-h-44 overflow-y-auto rounded-2xl border border-divider bg-well">{customerQuery.trim() ? customersLoading ? <div className="p-4 text-xs text-muted">Searching customers…</div> : customers.length ? (customers as any[]).slice(0, 6).map((customer: any) => <button key={customer.id} onClick={() => selectCustomer(customer)} className="flex w-full items-center justify-between gap-3 border-b border-divider px-4 py-3 text-left last:border-0 hover:bg-[#f0f7ff]"><span><b className="block text-sm">{customer.fullName}</b><span className="mt-1 block text-xs text-muted">{customer.phone || "No phone"}</span></span><span className="text-xs font-semibold text-accent">Select</span></button>) : <div className="p-4 text-xs text-muted">No profile found. Add the walk-in details below.</div> : <div className="p-4 text-xs text-muted">Start typing to find an existing customer.</div>}</div>{form.customerId ? <div className="mt-4 flex items-center justify-between rounded-2xl bg-success-bg px-4 py-3"><div><span className="block text-xs font-semibold text-success">Saved customer selected</span><span className="mt-1 block text-xs text-muted">ID {form.customerId}</span></div><SecondaryButton onClick={() => setForm((current) => ({ ...current, customerId: "" }))}>Change</SecondaryButton></div> : <div className="mt-5 grid gap-4"><div className="text-[10px] font-semibold uppercase tracking-[.14em] text-subtle">New walk-in</div><Field label="Full name"><TextField value={form.customerName} onChange={(event) => setForm({ ...form, customerName: event.target.value })} placeholder="Customer full name"/></Field><Field label="Phone number"><TextField value={form.customerPhone} onChange={(event) => setForm({ ...form, customerPhone: event.target.value })} inputMode="tel" placeholder="+968 …"/></Field></div>}</Surface>
        <Surface><div className="mb-5 flex items-start justify-between gap-3"><div><h2 className="font-serif text-2xl tracking-[-.04em]">Recent purchases</h2><p className="mt-1.5 text-xs leading-5 text-muted">Continuous ticket numbers and customer visits.</p></div><StatusPill>{groupedPurchases.length} purchases</StatusPill></div><SearchField value={ticketQuery} onChange={setTicketQuery} placeholder="Ticket number, customer, or phone"/><div className="mt-4">{purchasesLoading ? <div className="p-4 text-sm text-muted">Loading purchase history…</div> : groupedPurchases.length ? <TableFrame><TableHeader><div className="grid grid-cols-[1.25fr_.75fr_auto] gap-3"><span>Customer / ticket</span><span>Visit</span><span className="text-right">Total</span></div></TableHeader>{groupedPurchases.slice(0, 8).map((entry: any) => <TableRow key={entry.purchase.id} className="grid-cols-[1.25fr_.75fr_auto]"><div className="min-w-0"><div className="truncate text-sm font-medium">{entry.customer?.fullName || "Customer"}</div><div className="mt-1 truncate font-mono text-[10px] text-accent">{entry.lines.map((line: any) => line.ticketNumber).join(" · ")}</div></div><div className="text-xs text-muted">{dateLabel(entry.purchase.visitDate)}</div><b className="text-right text-sm">{money(entry.purchase.totalAmount)}</b></TableRow>)}</TableFrame> : <EmptyState title="No purchases yet" description="Issued purchases will appear here for quick lookup."/>}</div></Surface>
      </div>
      <Surface className="h-fit"><div className="mb-5 flex items-start justify-between gap-3"><div><h2 className="font-serif text-2xl tracking-[-.04em]">Visitor lines</h2><p className="mt-1.5 text-xs leading-5 text-muted">Choose the correct type strictly by pool use, not by age or relationship.</p></div><StatusPill tone={validLines.length === lines.length ? "success" : "warning"}>{validLines.length}/{lines.length} ready</StatusPill></div><div className="grid gap-3">{lines.map((line, index) => <div key={line.id} className="rounded-2xl border border-divider bg-well p-4"><div className="mb-3 flex items-center justify-between"><b className="text-sm">Visitor {index + 1}</b><button onClick={() => removeLine(line.id)} aria-label={`Remove visitor ${index + 1}`} className="rounded-full p-2 text-muted hover:bg-danger-bg hover:text-danger"><Trash2 size={14}/></button></div><div className="grid gap-3 sm:grid-cols-2"><Field label="Ticket type"><SelectField value={line.ticketType} onChange={(event) => updateLine(line.id, { ticketType: event.target.value as TicketLine["ticketType"], rateId: "" })}><option value="waterpark">Waterpark · uses pool</option><option value="companion">Companion · does not use pool</option></SelectField></Field><Field label="Approved base price"><SelectField value={line.rateId} onChange={(event) => updateLine(line.id, { rateId: event.target.value })}><option value="">Choose {line.ticketType} price</option>{rates.filter((rate) => rate.ticketType === line.ticketType).map((rate) => <option key={rate.id} value={rate.id}>{rate.name} · {money(rate.unitPrice)}</option>)}</SelectField></Field><Field label="Free entry category"><SelectField value={line.freeEntryCategory} onChange={(event) => updateLine(line.id, { freeEntryCategory: event.target.value as TicketLine["freeEntryCategory"] })}><option value="">Chargeable visitor</option><option value="under_two">Under 2 years old</option><option value="person_of_determination">Person of determination</option><option value="senior">Retiree / senior citizen</option></SelectField></Field></div><p className="mt-3 text-[11px] leading-4 text-muted">Free lines are issued for headcount but excluded from the group-discount count.</p></div>)}<SecondaryButton onClick={addLine}><Plus size={15} className="mr-2"/>Add visitor line</SecondaryButton></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><Field label="Visit date"><TextField type="date" value={form.visitDate} onChange={(event) => setForm({ ...form, visitDate: event.target.value })}/></Field><Field label="Payment method"><SelectField value={form.paymentMethod} onChange={(event) => setForm({ ...form, paymentMethod: event.target.value as FormState["paymentMethod"] })}><option value="cash">Cash</option><option value="card">Card</option><option value="bank">Bank transfer</option><option value="mixed">Mixed</option></SelectField></Field></div><div className="mt-5 rounded-[22px] bg-ink p-5 text-white"><div className="flex items-start justify-between gap-3"><div><div className="text-[10px] font-semibold uppercase tracking-[.16em] text-[#9abff2]">PRD price preview</div><div className="mt-2 font-serif text-4xl tracking-[-.05em]">{money(pricing?.totalAmount || 0)}</div></div><StatusPill tone={pricing ? "success" : "neutral"}>{pricing ? "Ready" : "Complete lines"}</StatusPill></div>{pricing && <div className="mt-4 border-t border-white/15 pt-3 text-xs text-[#d6d6da]"><div className="flex justify-between py-1"><span>Base subtotal</span><span>{money(pricing.baseSubtotal)}</span></div><div className="flex justify-between py-1"><span>Group discount ({pricing.discountPercentage}%)</span><span>−{money(pricing.discountAmount)}</span></div><div className="flex justify-between py-1"><span>VAT (5%) after discount</span><span>{money(pricing.vatAmount)}</span></div>{pricing.fees?.map((fee: any) => <div key={fee.code} className="flex justify-between py-1"><span>{fee.label}</span><span>{money(fee.amount)}</span></div>)}<div className="mt-2 border-t border-white/15 pt-2">{pricing.lines.map((line: any, index: number) => <div key={`${line.rateId}-${index}`} className="flex justify-between py-1"><span>{line.label}{line.freeEntryCategory ? ` · ${freeLabels[line.freeEntryCategory]}` : ""}</span><span>{money(line.totalAmount)}</span></div>)}</div></div>}</div><div className="mt-5"><Field label="Note (optional)"><Textarea value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} placeholder="Short note for this purchase" className="min-h-[86px] rounded-xl border-line bg-well"/></Field></div><div className="mt-5 flex flex-wrap gap-2 border-t border-divider pt-5"><PrimaryButton onClick={issuePurchase} pending={issue.isPending} disabled={!pricing || previewLines.length !== lines.length}>Confirm & issue purchase <Ticket size={15} className="ml-2"/></PrimaryButton>{created && <><SecondaryButton onClick={() => printReceipt("80")}><Printer size={14} className="mr-2"/>80 mm receipt</SecondaryButton><SecondaryButton onClick={() => printReceipt("58")}><Printer size={14} className="mr-2"/>58 mm</SecondaryButton></>}</div>{created && <div className="mt-5 rounded-2xl border border-[#cbead5] bg-[#effaf2] p-4"><StatusPill tone="success">Purchase ready</StatusPill><div className="mt-2 font-mono text-lg font-semibold text-ink">{created.lines.map((line: any) => line.ticketNumber).join(" · ")}</div><p className="mt-1 text-xs leading-5 text-muted">Print the normal receipt for the customer. Ticket numbers continue across all visits.</p></div>}</Surface>
    </div>
    {created && <TicketReceipt data={toReceiptData(created)} width={receiptWidth}/>}
  </>;
}
