import { trpc } from "@/lib/trpc";
import { ArrowRight, CheckCircle2, FileSpreadsheet, ShieldCheck, Upload, WandSparkles } from "lucide-react";
import { useLocation } from "wouter";

const join = (...values: Array<string | false | undefined>) => values.filter(Boolean).join(" ");
function Card({ children, className = "" }: { children: React.ReactNode; className?: string }) { return <section className={join("rounded-[22px] border border-black/[.06] bg-white p-5 shadow-[0_8px_26px_rgba(0,0,0,.045)] md:p-6", className)}>{children}</section>; }
function Metric({ label, value, note, tone = "blue" }: { label: string; value: string; note: string; tone?: "blue" | "amber" | "green" }) { const bg = tone === "green" ? "bg-[#eaf8ef] text-[#248a3d]" : tone === "amber" ? "bg-[#fff4d6] text-[#9a6700]" : "bg-[#e8f2ff] text-[#0071e3]"; return <div className={join("rounded-2xl p-4", bg)}><div className="text-[11px] font-medium">{label}</div><div className="mt-2 font-serif text-3xl tracking-[-.04em] text-[#1d1d1f]">{value}</div><div className="mt-1 text-[11px] leading-4 text-[#6e6e73]">{note}</div></div>; }
function Pill({ children, tone = "slate" }: { children: React.ReactNode; tone?: "green" | "amber" | "slate" }) { const color = tone === "green" ? "bg-[#eaf8ef] text-[#248a3d]" : tone === "amber" ? "bg-[#fff4d6] text-[#9a6700]" : "bg-[#f2f2f7] text-[#515154]"; return <span className={join("inline-flex rounded-full px-2.5 py-1 text-[10px] font-semibold", color)}>{children}</span>; }

export default function WorkbookMigrationPage() {
  const [, setLocation] = useLocation();
  const { data: imports = [], isLoading } = trpc.platform.finance.workbookImports.useQuery();
  return <><div className="mb-7 flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div><div className="text-[10px] font-semibold uppercase tracking-[.18em] text-[#0071e3]">Governance & transition</div><h1 className="mt-2 font-serif text-4xl tracking-[-.055em] text-[#1d1d1f] md:text-5xl">Workbook migration</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-[#6e6e73]">Move the resort from spreadsheet dependency to an auditable operating record—one approved source at a time.</p></div><Pill tone="amber">Approval required before import</Pill></div><Card className="!border-[#cce5ff] !bg-gradient-to-br !from-[#f0f7ff] !to-white"><div className="flex flex-col gap-6 lg:flex-row lg:items-center lg:justify-between"><div className="flex items-start gap-4"><div className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-[#0071e3] text-white shadow-[0_10px_22px_rgba(0,113,227,.2)]"><FileSpreadsheet size={22}/></div><div><div className="text-[11px] font-semibold uppercase tracking-[.14em] text-[#0071e3]">Source archive</div><h2 className="mt-1 font-serif text-2xl tracking-[-.03em]">AR-Revenue-2026-15082026.xlsx</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-[#6e6e73]">The original workbook is preserved as a reference. The system will preview, reconcile, and label imported rows instead of copying totals or formulas into live ledgers.</p></div></div><div className="flex shrink-0 gap-2"><button onClick={() => window.alert("Dry-run import preview is the next controlled step. Select the canonical source sheets and approve the mapping before loading rows.")} className="inline-flex items-center rounded-full bg-[#0071e3] px-4 py-2.5 text-xs font-semibold text-white shadow-[0_7px_17px_rgba(0,113,227,.2)]">Start dry run <ArrowRight size={14} className="ml-2"/></button></div></div></Card><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Worksheets" value="38" note="Source workbook structure"/><Metric label="Rendered pages" value="108" note="Reference archive size" tone="amber"/><Metric label="Duplicate set" value="19 pairs" note="Tabs marked (2) require review" tone="amber"/><Metric label="Rows imported" value="0" note="No production rows loaded" tone="green"/></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.1fr_.9fr]"><Card><div className="flex items-start justify-between gap-3"><div><h2 className="font-serif text-[22px] tracking-[-.03em]">Recommended import boundary</h2><p className="mt-1.5 text-xs leading-5 text-[#6e6e73]">A small, high-value first migration that replaces repeated daily spreadsheet work.</p></div><ShieldCheck className="text-[#248a3d]" size={20}/></div><div className="mt-5 space-y-3"><div className="flex items-start gap-3 rounded-2xl bg-[#eaf8ef] p-3"><CheckCircle2 className="mt-0.5 shrink-0 text-[#248a3d]" size={17}/><div><div className="text-sm font-semibold">Import after approval</div><p className="mt-1 text-xs leading-5 text-[#6e6e73]">Verified ticket/revenue rows, customer fields, expense categories, expenses, and approved cash-settlement rows.</p></div></div><div className="flex items-start gap-3 rounded-2xl bg-[#f2f2f7] p-3"><FileSpreadsheet className="mt-0.5 shrink-0 text-[#515154]" size={17}/><div><div className="text-sm font-semibold">Keep as reference initially</div><p className="mt-1 text-xs leading-5 text-[#6e6e73]">Totals, formulas, subtotals, salaries, assets, budgets, projects, journals, and petty-cash tabs.</p></div></div><div className="flex items-start gap-3 rounded-2xl bg-[#fff4d6] p-3"><WandSparkles className="mt-0.5 shrink-0 text-[#9a6700]" size={17}/><div><div className="text-sm font-semibold">Resolve duplicate tabs</div><p className="mt-1 text-xs leading-5 text-[#6e6e73]">Confirm whether the `(2)` sheets are backups, revisions, or accidental copies before choosing a canonical source.</p></div></div></div></Card><Card><h2 className="font-serif text-[22px] tracking-[-.03em]">Migration readiness</h2><p className="mt-1.5 text-xs leading-5 text-[#6e6e73]">This keeps the launch transparent: the client can see what is ready, what is blocked, and what was last recorded.</p><div className="mt-5 space-y-3"><div className="flex items-center justify-between rounded-xl border border-[#e5e5ea] p-3"><span className="text-sm">Source archive preserved</span><Pill tone="green">Ready</Pill></div><div className="flex items-center justify-between rounded-xl border border-[#e5e5ea] p-3"><span className="text-sm">Field mapping approved</span><Pill tone="amber">Pending</Pill></div><div className="flex items-center justify-between rounded-xl border border-[#e5e5ea] p-3"><span className="text-sm">Dry-run reconciliation</span><Pill tone="amber">Pending</Pill></div><div className="flex items-center justify-between rounded-xl border border-[#e5e5ea] p-3"><span className="text-sm">Production import</span><Pill>Not started</Pill></div></div></Card></div><Card className="mt-6"><div className="flex items-start justify-between gap-3"><div><h2 className="font-serif text-[22px] tracking-[-.03em]">Import history</h2><p className="mt-1.5 text-xs leading-5 text-[#6e6e73]">Approved imports will be recorded with source name, mapping, row count, and operator.</p></div><Upload className="text-[#86868b]" size={20}/></div>{isLoading ? <div className="mt-5 animate-pulse text-sm text-[#6e6e73]">Loading import history…</div> : imports.length ? <div className="mt-5 overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="text-[10px] uppercase tracking-wide text-[#86868b]"><tr><th className="pb-3">Source file</th><th className="pb-3">Rows</th><th className="pb-3">Imported</th><th className="pb-3">Mapping</th></tr></thead><tbody>{(imports as any[]).map((entry) => <tr key={entry.id} className="border-t border-[#e5e5ea]"><td className="py-3 font-medium">{entry.fileName}</td><td>{entry.rowsImported}</td><td>{new Date(entry.importedAt || entry.createdAt).toLocaleDateString("en-GB")}</td><td className="max-w-[300px] truncate text-xs text-[#6e6e73]">{entry.mapping || "—"}</td></tr>)}</tbody></table></div> : <div className="mt-5 rounded-2xl border border-dashed border-[#d2d2d7] bg-[#fbfbfd] p-7 text-center text-sm text-[#6e6e73]">No import has been recorded. That is intentional until the source sheets and mapping are approved.</div>}<div className="mt-5 flex flex-wrap gap-2"><button onClick={() => setLocation("/administration")} className="rounded-full bg-[#f2f2f7] px-4 py-2.5 text-xs font-semibold text-[#1d1d1f]">Back to administration</button><button onClick={() => window.alert("Use the attached migration profile to prepare the first mapping review.")} className="rounded-full bg-[#0071e3] px-4 py-2.5 text-xs font-semibold text-white">Review source profile</button></div></Card></>;
}
